import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const InsightScreen = () => {
  return (
    <SafeAreaView>
      <Text>InsightScreen iOS</Text>
    </SafeAreaView>
  );
};

export default InsightScreen;
