import React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const SettingScreen = () => {
  return (
    <SafeAreaView>
      <Text>SettingScreen iOS</Text>
    </SafeAreaView>
  );
};

export default SettingScreen;
